# Mindtrack
# README — Ejecución de los scrapers

## Requisitos
Estos scripts fueron probados en el siguiente entorno:

- macOS
- Safari
- SafariDriver
- Python 3

Antes de ejecutarlos, instalar las dependencias necesarias con:

```bash
pip install selenium beautifulsoup4